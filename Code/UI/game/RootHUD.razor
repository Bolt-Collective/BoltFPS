@using Sandbox.UI
@inherits PanelComponent
@namespace Seekers

<root>
	<PlayerInfo/>
	<WeaponWheel/>
	<SpectatorPanel/>
	<ServerVersion/>
</root>

@code
{
	public static RootHUD Instance { get; set; }

	protected override void OnAwake()
	{
		base.OnAwake();

		Instance = this;
	}

	protected override void OnStart()
	{
		base.OnStart();

		Panel.FindRootPanel().StyleSheet.Add( StyleSheet.FromFile( "UI/_styles.scss" ) );
	}
}
